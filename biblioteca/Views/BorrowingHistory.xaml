<UserControl x:Class="biblioteca.Views.BorrowingHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">

        <StackPanel>

            <TextBlock Text="Borrowing History"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0 0 0 10"/>

            <DataGrid ItemsSource="{Binding Loans}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Book"
                                        Binding="{Binding BookTitle}" />

                    <DataGridTextColumn Header="User"
                                        Binding="{Binding UserName}" />

                    <DataGridTextColumn Header="Borrow Date"
                                        Binding="{Binding BorrowDate}" />

                    <DataGridTextColumn Header="Return Date"
                                        Binding="{Binding ReturnDate}" />
                </DataGrid.Columns>

            </DataGrid>

        </StackPanel>

    </Grid>
</UserControl>