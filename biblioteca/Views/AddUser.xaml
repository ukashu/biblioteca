<Window x:Class="biblioteca.Views.AddUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:biblioteca.Views"
        mc:Ignorable="d"
        Title="AddUser" Height="450" Width="800">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Imię" Grid.Column="0"/>
        <TextBox x:Name="FirstNameBox" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="Nazwisko" Grid.Row="1"/>
        <TextBox x:Name="LastNameBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="Email" Grid.Row="2"/>
        <TextBox x:Name="EmailBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="Telefon" Grid.Row="3"/>
        <TextBox x:Name="PhoneBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="Nr karty" Grid.Row="4"/>
        <TextBox x:Name="CardNumberBox" Grid.Row="4" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="Notatki" Grid.Row="5"/>
        <TextBox x:Name="NotesBox" Grid.Row="5" Grid.Column="1" Margin="0,0,0,10" Height="80" AcceptsReturn="True" TextWrapping="Wrap"/>

        <Button Content="Dodaj użytkownika"
                Grid.Row="6"
                Grid.ColumnSpan="2"
                Margin="10,10,10,10"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Click="AddUser_Click"/>

    </Grid>
</Window>
