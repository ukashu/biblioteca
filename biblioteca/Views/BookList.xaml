<UserControl x:Class="biblioteca.Views.BookList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:biblioteca.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <ListView x:Name="booksListView" ItemsSource="{Binding Books}" SelectedItem="{Binding SelectedBook, Mode=TwoWay}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <EventSetter Event="MouseDoubleClick" Handler="BookItem_MouseDoubleClick"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                                <Setter Property="Background" Value="LightCoral"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Title}" Width="220">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="Title" Click="booksListViewColumnHeader_Click">Tytuł</GridViewColumnHeader>
                            </GridViewColumn.Header>
                        </GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Author}" Width="140">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="Author" Click="booksListViewColumnHeader_Click">Autor</GridViewColumnHeader>
                            </GridViewColumn.Header>
                        </GridViewColumn>
                        <GridViewColumn Header="Wypożyczono" Width="160">
                            <GridViewColumn.DisplayMemberBinding>
                                <Binding Path="CurrentBorrowDate" StringFormat="dd.MM.yyyy"/>
                            </GridViewColumn.DisplayMemberBinding>
                        </GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding PublicationYear}" Width="Auto">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="PublicationYear" Click="booksListViewColumnHeader_Click">Wydanie</GridViewColumnHeader>
                            </GridViewColumn.Header>
                        </GridViewColumn>
                        <GridViewColumn Header="Dostępna" DisplayMemberBinding="{Binding IsAvailable}" Width="200"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </ScrollViewer>

        <Border Grid.Column="1" Margin="10,0,0,0" Padding="10" Background="#f9f9f9" BorderBrush="#ddd" BorderThickness="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedItem, ElementName=booksListView}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel DataContext="{Binding SelectedItem, ElementName=booksListView}">
                <TextBlock Text="Szczegóły" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                <Image Source="{Binding CoverImagePath}" Width="60" Height="100" Stretch="Uniform"/>
                <TextBlock Text="Opis: " FontWeight="Bold"/>
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                <TextBlock Text="Gatunek: " FontWeight="Bold"/>
                <TextBlock Text="{Binding Genre}" Margin="0,0,0,10"/>
                <TextBlock Text="ISBN: " FontWeight="Bold"/>
                <TextBlock Text="{Binding Signature}" Margin="0,0,0,10"/>
                <TextBlock Text="Wydanie: " FontWeight="Bold"/>
                <TextBlock Text="{Binding PublicationYear}" Margin="0,0,0,10"/>
                <TextBlock Text="Wypożyczone przez:" FontWeight="Bold"/>
                <TextBlock Text="{Binding DataContext.CurrentBorrower, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </StackPanel>
        </Border>
        
        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="2">
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Tytuł" Margin="0,0,0,2"/>
                    <TextBox Width="100" Height="30" VerticalContentAlignment="Center" Name="TitleFilterTextBox" TextChanged="TitleFilterTextBox_TextChanged"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Autor" Margin="0,0,0,2"/>
                    <TextBox Width="100" Height="30" VerticalContentAlignment="Center" Name="AuthorFilterTextBox" TextChanged="AuthorFilterTextBox_TextChanged"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="ISBN" Margin="0,0,0,2"/>
                    <TextBox Width="100" Height="30" VerticalContentAlignment="Center" Name="SignatureFilterTextBox" TextChanged="SignatureFilterTextBox_TextChanged"/>
                </StackPanel>
                <CheckBox x:Name="OverdueOnlyCheckBox"
                    Content="Tylko przeterminowane"
                    Margin="10,22,0,0"
                    VerticalAlignment="Center"
                    Checked="OverdueOnlyCheckBox_Changed"
                    Unchecked="OverdueOnlyCheckBox_Changed"/>
            </StackPanel>

            <Button Grid.Column="1" Name="add_book_button" Content="Dodaj" HorizontalAlignment="Right" Command="{Binding AddBookWithDialogCommand}"/>
        </Grid>
    </Grid>
</UserControl>
