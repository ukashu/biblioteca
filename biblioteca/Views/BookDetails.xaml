<Window x:Class="biblioteca.Views.BookDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:biblioteca.Views"
        mc:Ignorable="d"
        Title="BookDetails" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Title" Grid.Row="0" Grid.Column="0"/>
        <TextBlock Text="{Binding Title}" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <Label Content="Author" Grid.Row="1" Grid.Column="0"/>
        <TextBlock Text="{Binding Author}" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Label Content="Publication Year" Grid.Row="2" Grid.Column="0"/>
        <TextBlock Text="{Binding PublicationYear}" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <Label Content="Genre" Grid.Row="3" Grid.Column="0"/>
        <TextBlock Text="{Binding Genre}" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <Label Content="Signature" Grid.Row="4" Grid.Column="0"/>
        <TextBlock Text="{Binding Signature}" Grid.Row="4" Grid.Column="1" Margin="5"/>

        <Label Content="Description" Grid.Row="5" Grid.Column="0"/>
        <TextBlock Text="{Binding Description}"
                   Grid.Row="5"
                   Grid.Column="1"
                   Margin="5"
                   TextWrapping="Wrap"/>

        <Label Content="Is Available" Grid.Row="6" Grid.Column="0"/>
        <TextBlock Text="{Binding IsAvailable}" Grid.Row="6" Grid.Column="1" Margin="5"/>

    </Grid>
</Window>
