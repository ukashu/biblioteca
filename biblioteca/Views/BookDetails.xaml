<Window x:Class="biblioteca.Views.BookDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:biblioteca.Views"
        mc:Ignorable="d"
        Title="Szczegóły książki" Height="450" Width="800" MinWidth="400" MinHeight="350">
    <Grid>
        <Grid Margin="10" x:Name="ViewDetailsPanel">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <Label Content="Tytuł" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBlock x:Name="TitleText" Text="{Binding Title}" Grid.Row="0" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

            <!-- Author -->
            <Label Content="Autor" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBlock x:Name="AuthorText" Text="{Binding Author}" Grid.Row="1" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

            <!-- Publication Year -->
            <Label Content="Rok publikacji" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBlock x:Name="PublicationYearText" Text="{Binding PublicationYear}" Grid.Row="2" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

            <!-- Genre -->
            <Label Content="Gatunek" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBlock x:Name="GenreText" Text="{Binding Genre}" Grid.Row="3" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

            <!-- Signature -->
            <Label Content="ISBN" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBlock x:Name="SignatureText" Text="{Binding Signature}" Grid.Row="4" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

            <!-- Description -->
            <Label Content="Opis" Grid.Row="5" Grid.Column="0"/>
            <TextBlock x:Name="DescriptionText"
                   Text="{Binding Description}"
                   Grid.Row="5"
                   Grid.Column="1"
                   Margin="5"
                   TextWrapping="Wrap"/>

            <!-- Is Available -->
            <Label Content="Dostępna" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBlock x:Name="IsAvailableText" Text="{Binding IsAvailable}" Grid.Row="6" Grid.Column="1" Margin="5" VerticalAlignment="Center"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="7"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
                <Button x:Name="DeleteButton" Content="Usuń" Width="80" Margin="5" Click="DeleteButton_Click"/>
                <Button x:Name="EditButton" Content="Edytuj" Width="80" Margin="5" Click="EditButton_Click"/>
            </StackPanel>
        </Grid>
        <Grid Margin="10" x:Name="EditDetailsPanel" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <Label Content="Tytuł" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="TitleBox" Grid.Row="0" Grid.Column="1" Margin="5" />

            <!-- Author -->
            <Label Content="Autor" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="AuthorBox" Grid.Row="1" Grid.Column="1" Margin="5" />

            <!-- Publication Year -->
            <Label Content="Rok publikacji" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="PublicationYearBox" Grid.Row="2" Grid.Column="1" Margin="5" />

            <!-- Genre -->
            <Label Content="Gatunek" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="GenreBox" Grid.Row="3" Grid.Column="1" Margin="5" />

            <!-- Signature -->
            <Label Content="ISBN" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="SignatureBox" Grid.Row="4" Grid.Column="1" Margin="5" />

            <!-- Description -->
            <Label Content="Opis" Grid.Row="5" Grid.Column="0"/>
            <TextBox x:Name="DescriptionBox"
                 Grid.Row="5"
                 Grid.Column="1"
                 Margin="5"
                 MinHeight="60"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 />

            <!-- Is Available -->
            <Label Content="Dostępna" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center"/>
            <CheckBox x:Name="IsAvailableBox" Grid.Row="6" Grid.Column="1" Margin="5" />

            <!-- Buttons -->
            <StackPanel Grid.Row="7"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
                <Button x:Name="SaveButton" Content="Zapisz" Width="80" Margin="5" Click="SaveButton_Click"/>
                <Button x:Name="CancelButton" Content="Anuluj" Width="80" Margin="5" Click="CancelButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
